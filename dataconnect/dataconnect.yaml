specVersion: "v1beta"
serviceId: "recicle"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "recicle-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
